name: Build Mac
'on': push
jobs:
  build-mac:
    runs-on: macos-26
    steps:
      - uses: actions/checkout@v4
      - name: SDL2
        run: brew update && brew install sdl2
      - name: make
        run: make
